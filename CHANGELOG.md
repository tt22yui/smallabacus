# 更新日志

本项目所有值得记录的变更都会汇总在此文件中。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

> 每次发布构建（打 `v*` tag）后，构建脚本会自动汇总自上一个 tag 以来的提交记录，并写入对应版本的 GitHub Release 说明。

## \[未发布]

## [0.1.2] - 2026-09-02

### 新增

- 标题栏显示应用版本徽标（自动读取运行版本号）。

### 其他

- 版本号同步更新为 0.1.2。
- 新增 AI 协作规范 `AGENTS.md`。

## \[0.1.0] - 2026-09-01

### 新增

- 10 以内加减法试卷生成：题目范围 3\~50 任意调节（默认 10）。

- 自定义题型分布：加法 / 减法占比可调（10%\~90%）。

- 灵活的排版布局：每行题数、每页行数、总页数自由设置，实时预览。

- 智能出题引擎：加法池 `a+b ≤ N`、减法池 `a ≥ b` 且结果非负；卷内不重复、相邻不重复；题量超限自动降级。

- 卷头信息：姓名、日期（留空自动填当天）。

- 多样背景：内置多套背景，支持上传自定义图片作为卷面水印底图。

- 一键换题：重新洗牌生成新卷面。

- 打印 / 导出 PDF：调用系统打印对话框，可按 A4 输出。

- 设置自动记忆：偏好本地持久化，重启后保留。

