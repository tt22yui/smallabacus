# AGENTS.md — AI 协作规范

本文件帮助 AI 编程助手（TraeCode、Claude Code、Cursor 等）快速理解「小算盘 · SmallAbacus」并遵循项目约定。开始新任务前请先阅读本文件与 `README.md`。

## 项目定位

10 以内加减法小学数学口算试卷生成器桌面应用。基于 **Tauri v2**（Rust 后端 + React/TypeScript 前端 + Tailwind CSS v4 + Vite）。

核心业务：灵活出题 → 实时预览 → 打印 / 导出 PDF。

## 角色与职责

本项目涉及两类用户角色，外加 AI 助手自身的角色，设计和实现时请始终明确"当前在为谁服务"：

- **出题者（教师 / 家长）**：通过左侧设置面板配置题目范围、题型分布、布局、卷头信息与背景，生成试卷并打印。

- **答题者（学生，未成年人）**：使用**打印出的纸质卷面**完成练习，不直接操作本应用界面。

- **AI 助手（Tauri 桌面应用全栈工程师）**：作为本项目的开发与维护者，以 **Rust 后端 + React 前端**全栈工程师的视角工作，覆盖出题引擎、UI/UX 与打印、自定义标题栏、窗口与主题、打包与 CI、隐私与开源合规等完整链路。

  - 接到任何改动时，先用"全栈工程师"视角审视：该改动对出题质量、界面一致性、打印边界、构建与打包、隐私合规的影响，不只见点不见面。

  - 完成代码后按本文件「验收 Checklist」自检（类型检查、最小窗口布局、主题一致性、打印不破坏）。

  - 所有产出（题目文案、界面、代码）都应符合"面向小学低年级学生与家长"的定位，内容健康、题意清晰、无歧义。

  - 涉及隐私、未成年人数据、公开仓库提交时，严格遵循下方「隐私与内容安全」章节。

> 设计任何新交互或内容时，先判断面向哪个角色：面向出题者的功能应高效、清晰；面向答题者的内容应简单、规范、适合儿童。

## 技术栈与关键依赖

- **后端**：Rust（Tauri v2），入口位于 `src-tauri/src/`。

- **前端**：React 19 + TypeScript + Tailwind CSS v4（通过 `@tailwindcss/vite` 插件）+ Vite。

- **Tauri 插件**：`@tauri-apps/plugin-dialog`、`@tauri-apps/plugin-fs`（本地背景图片上传）。

- **无**路由库、无状态管理库，均为单页组件内 `useState` 管理。

## 目录结构

| 路径                            | 职责                                                     |
| ----------------------------- | ------------------------------------------------------ |
| `src/App.tsx`                 | 主界面：设置面板 + 试卷实时预览 + 自定义标题栏                             |
| `src/generator.ts`            | 出题引擎（随机、卷内/相邻不重复、题型占比、降级处理）                            |
| `src/backgrounds.ts`          | 内置背景定义                                                 |
| `src/types.ts`                | TypeScript 类型与默认设置常量                                   |
| `src-tauri/`                  | Tauri 后端：`src/` Rust 入口、`tauri.conf.json`、`Cargo.toml` |
| `.github/workflows/build.yml` | GitHub Actions 自动构建与发布                                 |
| `public/images/bg/`           | 内置背景 SVG 资源                                            |

## 常用命令

```bash
npm install                      # 安装前端依赖
npm run tauri dev                # 开发模式（热更新 + 调试窗口）
npm run build                    # 前端类型检查(tsc) + 打包(仅前端)
npm run tauri build              # 完整桌面应用打包
```

- 前端构建脚本为 `tsc && vite build`，**必须保证** **`tsc`** **类型检查通过**。

- 修改 Rust 代码后需重新 `npm run tauri dev` 才会重新编译 backend。

## 硬性约束（务必遵守）

1. **版本号五处保持一致**：修改版本号时必须同步更新
   `package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、
   `src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json`。
2. **GitHub Actions 仅在推送** **`v*`** **格式 tag 时触发**构建（如 `v0.1.0`），非 tag 推送不构建。
3. 构建产物自动上传到对应 tag 的 GitHub Release **草稿**，需人工确认发布；不要通过 commit 到 `main` 触发发布。
4. 只构建 Windows（`windows-latest`）与 macOS（`macos-latest`）两个平台。
5. 产物仅包含 Windows 绿色 zip 安装包 与 macOS 的 dmg 包。

## 隐私与内容安全（未成年人保护）

因卷面面向小学低年级学生（未成年人），必须严格遵循以下约定：

- **数据本地优先**：卷头信息（姓名、日期）与应用设置只用于**本地渲染、打印与本地持久化**。不得新增任何上传到网络的逻辑、遥测/埋点或第三方分析 SDK。

- **最小化收集**：只允许收集出题者填写的必要字段（如姓名、日期）。不得请求、存储或处理学生的敏感信息（学号、身份证号、家庭住址、就读学校等）。

- **日志与文档脱敏**：开发、调试、示例或文档中不得出现真实学生个人可识别信息；需要示例时一律使用占位（如「小明」、空姓名）。

- **内容适合儿童**：题目、文案、配图、背景、动画、音效等一切产出须符合儿童教育定位。禁止任何暴力、恐怖、惊悚、血腥、不当或与教学无关的内容；涉及未成年人场景一律保持积极、健康、正向。

- **不面向儿童做任何推销 / 诱导**：不得在界面中加入面向儿童的推销、广告、诱导分享或留存机制。

- **无远程依赖**：核心功能须离线可用，不依赖线上服务或云端出题。

- **开源提交隐私检查**：本仓库公开（MIT License），**每次提交前**必须检查暂存内容，确保不泄露任何敏感信息：

  - 不提交 `.env`、密钥、令牌、私钥、密码、API Key 等凭据；此类文件一律列入 `.gitignore`。

  - 不提交真实学生 / 用户的个人可识别信息（真实姓名、联系方式、学校、账号等）；示例一律使用占位。

  - 不提交本机绝对路径、开发者用户名、内网地址等环境信息。

  - 提交前确认日志、截图、生成数据、测试样例中均已脱敏。

  - 若存在"已提交后才发现泄露"的情况，立即提醒用户删除并改用 `git filter-branch` / `git-filter-repo` 清理历史，并建议轮换任何暴露的密钥。

## 命名与代码风格

- **注释统一使用中文**（含代码内注释与文档）。

- **Rust**：`snake_case`（文件、函数、变量）。

- **前端组件**：文件名大驼峰，如 `App.tsx`；变量/函数使用 `camelCase`。

- 业务相关既有文件保持既有命名（`generator.ts`、`backgrounds.ts`、`types.ts`），新增请遵循此风格。

- 类型优先集中定义在 `src/types.ts`，跨文件复用请从此引入，避免散落重复定义。

## 前端 UI 约定

- **样式使用 Tailwind CSS v4**，通过 Tailwind 工具类写在 JSX 上；不要引入 CSS 框架或手写大量新样式。

- **暗黑模式**：统一通过 `dark:` 变体实现，底色为 `stone` 系列；亮色为 `cream-50`（自定主题色）。

- **主题色为橙色系（算盘主题）**，强调色统一使用现有品牌色。

- **自定义标题栏**（`decorations: false`）：可拖拽区域必须标记 `data-tauri-drag-region`，窗口控制按钮（最小化/最大化/关闭）位于标题栏右侧；最大化状态通过 `getCurrentWindow().isMaximized()` 动态监听。

- **打印**：打印区域统一加 `no-print` 类即可在打印时隐藏（如设置面板、标题栏）；卷面本体保持可打印。

- 设置面板按逻辑用卡片组件分组（题目、题型分布、布局、卷头信息、背景），圆角 + 细边框 + 半透明白底。

- 数值调整统一用步进式（− 值 +）组件，不带单位后缀、不显示浏览器原生箭头。

## 出题引擎逻辑（改动需小心）

位于 `src/generator.ts`：

- 加法池 `a+b ≤ N`，减法池 `a ≥ b` 且结果非负。

- 卷内题目不重复、相邻题目不重复。

- 题量超出组合上限时自动降级处理，并向用户给出提示。

- 修改任何出题/去重逻辑后，请通过界面「一键换题」手动验证结果是否符合预期。

## 验收 Checklist

- [ ] 前端 `npm run build`（含 `tsc`）通过，无类型错误。

- [ ] 布局在 `900 × 640`（窗口最小尺寸）下仍可用、不破版。

- [ ] 暗黑 / 亮色两种主题下预览与打印表现一致。

- [ ] 改动不破坏打印（`no-print` 边界正确）。

